# AI 协作强约束规则（2D游戏项目）

> 本文件约束 Claude 等 AI 助手的回答方式和代码修改行为。违反以下规则的回答将被视为无效。

## 1. 回答格式约束（必须遵守）

每次回答涉及代码时，必须按以下结构输出：
【修改类型】: (新增功能 / 修复bug / 重构 / 文档)
【涉及PR】: PR编号（如果已知）
【修改文件】: 列出所有将修改的文件路径（不超过3个）
【代码变更】:
--- a/src/xxx.py
+++ b/src/xxx.py
... 具体diff ...
【测试方式】: 如何验证改动
【依赖变更】: 如果有新的第三方库，必须在此说明

**禁止输出**：
- 超过3个文件的单次改动（除非是创建新文件且内容极少）
- 没有 diff 形式的代码建议（必须给出精确修改）
- 模糊的"你可以这样写..."而不给具体代码

## 2. 文件分类强制规则

AI 只能在以下目录和文件范围内建议修改，不能越界：

| 目录 | 允许修改的内容 | 禁止修改的内容 |
|------|---------------|----------------|
| `src/core/` | 主循环、初始化、渲染抽象层 | 任何业务逻辑 |
| `src/entities/` | 玩家、敌人、子弹的属性与行为 | 系统级逻辑（碰撞、生成） |
| `src/systems/` | 碰撞、生成、血量、分数系统 | 直接修改实体类内部 |
| `src/ui/` | HUD、菜单、文本显示 | 核心游戏逻辑 |
| `src/utils/` | 资源加载、数学辅助函数 | 任何游戏特定逻辑 |
| `assets/` | 只读建议（不能直接修改二进制） | - |
| `tests/` | 单元测试代码 | 生产代码 |
| `README.md` | 文档更新 | 代码 |

**绝对禁止修改的文件**（手动维护，AI不碰）：
- `.github/pull_request_template.md`
- `docs/pr_plan.md`（PR计划表）
- `requirements.txt` / `package.json`（除非明确要求并说明版本）

## 3. PR 粒度约束

AI 建议的每个代码变更，必须能对应到一个 **单一功能的 PR**。判断标准：

- ✅ 允许：只改玩家的移动速度常量
- ✅ 允许：添加一个空函数框架（无实现）
- ❌ 禁止：同时改玩家移动 + 碰撞检测 + 分数显示
- ❌ 禁止：超过 50 行新增代码（除非是数据定义或注释）

如果功能需要大改，必须拆分为至少 3 个 PR，AI 需要分次输出。

## 4. 主分支稳定性约束

AI 绝不能建议：
- 会让 `main` 分支无法运行的代码（例如引用了未定义的变量）
- 删除正在被其他模块调用的函数（必须先标记 deprecated）
- 修改入口点 `main.py` 而不保留原有启动方式

## 5. 违规处理

如果 AI 违反上述任何一条，用户将回复"违规：<原因>"，AI 必须：
1. 承认违规
2. 重写符合规则的回答
3. 如果连续2次违规，停止输出代码，只输出"请重新描述需求"

---

**版本**: v1.0
**最后更新**: 2026-05-23
